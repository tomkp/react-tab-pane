{
  "name": "react-auto-suggest",
  "description": "React auto-suggest component",
  "main": "dist/auto-suggest.js",
  "version": "0.0.1",
  "repository": {
    "type": "git",
    "url": "https://github.com/tomkp/react-auto-suggest"
  },
  "author": "tomkp <tom@tomkp.com>",
  "keywords": [
    "react",
    "react-component",
    "auto-suggest"
  ],
  "devDependencies": {
    "babel-core": "^4.6.6",
    "babel-jest": "^4.0.0",
    "babelify": "^5.0.4",
    "browserify": "^9.0.3",
    "jest-cli": "^0.4.0"
  },
  "scripts": {
    "test": "jest",
    "demo": "browserify demo/Demo.js -t babelify --outfile demo/Bundle.js",
    "build": "browserify src/TabPane.js -t babelify --outfile dist/TabPane.js"
  },
  "dependencies": {
    "react": "^0.13.0-rc1"
  },
  "jest": {
    "rootDir": "./src",
    "scriptPreprocessor": "../preprocessor.js",
    "unmockedModulePathPatterns": [
      "react"
    ]
  }
}
